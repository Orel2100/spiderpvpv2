package com.jules.kitpvp.listeners;

import com.jules.kitpvp.KitPVP;
import org.bukkit.event.Listener;

public class TempBlocksListener implements Listener {

    private final KitPVP plugin;

    public TempBlocksListener(KitPVP plugin) {
        this.plugin = plugin;
    }

    // This will handle the removal of temporary blocks created by abilities.
    // For now, it's a placeholder.
}
